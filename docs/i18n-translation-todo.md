# Pending translations

Tracking new strings shipped with English values across all four locales
(en/fr/es/sk) so the i18n sync check stays green. These need proper
translations in the next translation batch.

Format: `KEY  — short note about context`

## Survey divergence — Phase 1 (DONE 2026-04-29)

All 22 keys (`CONFLICT.divergence*`, alignment bands, dimensional table
headers, `SURVEY.dimension*`) translated into fr / es / sk. The
divergenceDisclaimer copy in particular preserved the "different
experiences, not different truths" framing across all three languages.
Helena should still cast an eye over the FR rendering on her tenant
when it next deploys.

## Survey divergence — Phase 2 (DONE 2026-04-29)

`CONFLICT.subgroupsTitle / subgroupsIntro / subgroupsStackTooltip /
subgroupsDisclaimer / respondents / distinguishingItems`, plus the
admin policy keys `ADMIN.qualityPolicyTitle / qualityPolicyHint /
qualityThreshold / qualityThresholdDesc / longStringMaxFraction /
longStringMaxFractionDesc / saveQualityPolicy / qualityPolicySaved /
showSubgroupAnalysis / showSubgroupAnalysisDesc / minSubgroupN /
minSubgroupNDesc`. Translated into fr / es / sk in the same batch.
